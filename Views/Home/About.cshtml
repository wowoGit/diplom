@{
    ViewData["Title"] = "Privacy Policy";
    var med_list = ViewData["meds"] as List<Medication>;
}
<h1>@ViewData["Title"]</h1>

<p>Use this page to detail your site's privacy policy.</p>
@foreach(var item in med_list)
            {
                <p>@item.Name</p>
            }

